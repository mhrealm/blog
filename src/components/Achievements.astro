---
interface JuejinStats {
  follower_count: number;
  got_view_count: number;
  got_digg_count: number;
  post_article_count: number;
  level: number;
  power: number;
  updated_at: number;
}

const env = Astro.locals.runtime?.env;
let juejinStats: JuejinStats | null = null;

if (env?.JUEJIN_KV) {
  try {
    juejinStats = await env.JUEJIN_KV.get<JuejinStats>("user_stats", "json");
  } catch (e) {
    console.error("Failed to fetch Juejin stats:", e);
  }
}

function formatCount(num: number): string {
  if (num >= 10000) {
    return (num / 10000).toFixed(1) + "w";
  }
  return num.toLocaleString();
}

const stats = [
  {
    value: juejinStats
      ? `${formatCount(juejinStats.got_view_count)}+`
      : "100w+",
    label: "阅读量",
    description: "掘金平台",
    icon: "📊",
  },
  {
    value: juejinStats
      ? `${formatCount(juejinStats.follower_count)}+`
      : "3,000+",
    label: "粉丝",
    description: "持续增长中",
    icon: "👥",
  },
  {
    value: juejinStats
      ? `${formatCount(juejinStats.post_article_count)}+`
      : "50+",
    label: "技术文章",
    description: "原创分享",
    icon: "✍️",
  },
  {
    value: juejinStats ? `Lv.${juejinStats.level}` : "5+",
    label: "掘金等级",
    description: "持续创作", // Changed from "GitHub" "开源项目" to match Juejin context or keep mixed?
    // Original was "开源项目", user provided Juejin data.
    // "level": 6. Let's use Level instead of "Open Source Projects" if we are strictly showing Juejin stats?
    // OR keep "Open Source Projects" static?
    // User said "Update these changes in Achievements.astro".
    // The 4th item was "开源项目". The provided data has "level" and "power".
    // I will replace "开源项目" with "掘金等级" or "掘力值"?
    // Let's stick to "掘金等级" Lv.6 seems cool.
    icon: "🏅", // Rocket -> Medal
  },
];
---

<div class="py-16 sm:py-20 lg:py-24">
  <div class="text-center mb-10 sm:mb-12">
    <p
      class="text-xs sm:text-sm font-medium text-indigo-400 uppercase tracking-widest mb-2"
    >
      Achievements
    </p>
    <h2 class="text-2xl sm:text-3xl font-bold text-white">成就与影响力</h2>
    <p class="text-sm sm:text-base text-gh-muted mt-3 max-w-xl mx-auto">
      作为掘金平台的优秀作者，专注于前端技术分享
    </p>
  </div>

  <div class="grid grid-cols-2 lg:grid-cols-4 gap-3 sm:gap-4">
    {
      stats.map((stat) => (
        <div class="group relative">
          <div class="absolute -inset-0.5 bg-gradient-to-r from-indigo-500 to-purple-500 rounded-xl blur opacity-0 group-hover:opacity-20 transition duration-500" />
          <div class="relative p-4 sm:p-6 bg-gh-surface rounded-xl border border-gh-border text-center group-hover:border-gh-muted transition-all duration-300">
            <span class="text-2xl sm:text-3xl mb-2 sm:mb-3 block">
              {stat.icon}
            </span>
            <div class="text-xl sm:text-2xl lg:text-3xl font-bold text-white mb-1">
              {stat.value}
            </div>
            <div class="text-sm sm:text-base font-medium text-gh-text">
              {stat.label}
            </div>
            <div class="text-xs text-gh-muted mt-0.5">{stat.description}</div>
          </div>
        </div>
      ))
    }
  </div>

  <div class="text-center mt-8 sm:mt-10">
    <a
      href="https://juejin.cn/user/712139267641950"
      target="_blank"
      class="inline-flex items-center gap-2 text-sm text-indigo-400 hover:underline underline-offset-4"
    >
      <span>查看我的掘金主页</span>
      <svg
        class="w-4 h-4"
        fill="none"
        viewBox="0 0 24 24"
        stroke="currentColor"
        stroke-width="2"
      >
        <path
          stroke-linecap="round"
          stroke-linejoin="round"
          d="M10 6H6a2 2 0 00-2 2v10a2 2 0 002 2h10a2 2 0 002-2v-4M14 4h6m0 0v6m0-6L10 14"
        ></path>
      </svg>
    </a>
  </div>
</div>
